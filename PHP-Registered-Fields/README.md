ACF-PHP-Registered-Fields
===========================

Adds admin menu page which shows field groups registered via PHP code.

## Usage

1. Install and activate plugin
2. Find new admin page 'Custom Fields (PHP)'